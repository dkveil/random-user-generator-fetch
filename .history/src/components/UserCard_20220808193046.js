import styled from "styled-components";

const UserCardWrapper = styled.div`

`

const UserCard = () => {
    return(
        <UserCardWrapper>

        </UserCardWrapper>
    );
};

export default UserCard;
